<UserControl
    x:Class="OnRadio.App.Controls.PlayerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnRadio.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="320"
    d:DesignWidth="180"
    d:DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=Player}">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Command="{Binding OpenRadioListCommand}">List</Button>
        <TextBlock Grid.Row="1" Text="{Binding Radio.Title}"/>
        <Image Grid.Row="2" Source="{Binding Information.ThumbnailUrl}"></Image>
        <TextBlock Grid.Row="3" Text="{Binding Information.Title}"/>
        <Button Grid.Row="4" Command="{Binding TogglePlayPauseCommand}">Play/Pause</Button>
    </Grid>
</UserControl>
